box: node
services:
  
build:
  steps:
    - npm-install:
        strict-ssl: false
    
    # A comment
    -  npm-test
    -  script:
        name: some simple test!
        code: |-
          ech"line 1"
          echo "line 2"

  after-steps:
      - mzp/http-notify:
          url: http://requestb.in/1jhhshq1
